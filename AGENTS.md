# 股票盘面分析工作规则

## 上下文入口

每次处理新的投资问题时，先读取：

1. `docs/持仓说明.md`
2. `docs/交易体系.md`
3. `reports/latest.json`

这三份文件是压缩后的项目记忆和交易体系，不必重新翻全部历史对话。

## 触发条件

当用户发送股票、ETF、基金、指数、账户持仓或行情盘面截图时，必须按以下流程执行。

## 必须执行的分析流程

1. 先读取图片内容，识别标的代码、名称、当前价、成本、仓位、盈亏、盘口、分时或 K 线信息。
2. 结合用户已安装的投资大师 skill，例如 buffett-perspective、graham-perspective、lynch-perspective 等，选择适合该标的风格的框架。
3. 实时查询最新消息面、技术面、资金面、盘面热点新闻、板块表现。
4. 同时检索公开媒体评论、财经博主观点、社区讨论和机构看法，形成多空证据。
5. 把大师框架、实时盘面和多空信息放在一起交叉判断，不允许只凭单一看法下结论。

## 操作决策强制清单（每次给操作建议前必须逐项完成）

回答"买不买/卖不卖/加不加/减不减"之前，必须同时拿出以下六项证据，缺一不可：

1. 历史规律：查该标的历史走势、关键支撑压力、同板块历史涨跌规律，找可重复的模式。
2. 实时消息面：检索该标的及所在板块的利空/利好消息（政策、订单、业绩、公告）。
3. 技术面：MA、RSI、MACD、量能、趋势方向。
4. 盘面：当前价、分时、盘口、板块当日表现。
5. 市场情绪：股吧/社区/资金流向的情绪方向。
6. 媒体与博主看法：公开财经媒体、博主、机构研报的多空观点。

六项交叉判断后再给唯一结论；任何一项查不到就明说缺哪一项，而不是含糊带过。

## 分级分析规则（自动判断何时动用多智能体）

默认用 **L1 常规分析**（秒级、零额外成本）：实时行情 + 技术面 + 消息面 + 市场情绪 + 大师框架 + 历史规律，直接给结论、依据和走势预判。

只有满足以下任一条件时，才升级到 **L2 深度分析**（TradingAgents-CN 多智能体，约 2-5 分钟、耗 DeepSeek 余额 1-3 元）：

1. 用户问的是"买/卖/加/减/清仓"等要动真金白银的操作决策；
2. 模型上涨概率处于中性区（0.38~0.62）且多空证据互相矛盾；
3. 价格逼近关键位（支撑/压力/止盈/止损 的 ±1% 内）；
4. 该标的 24 小时内出现重大事件（政策、业绩、地缘、大单）；
5. 用户明确要求"给我最深度的分析"。

不满足以上条件时，用 L1 直接给结论，不浪费时间和余额。同一标的 24 小时内已跑过 L2 且无新事件时，复用缓存结论，不重复跑。

## 前瞻预判规则

1. 当用户问后续操作时，优先读取 `reports/latest.json` 中每只标的的 `prediction` 字段。
2. 必须把模型预测转化为可执行信息：上涨概率、预期收益、T+1 波动区间、触发价、置信度、舆情方向和大师判定。
3. 只有上涨概率达到 `0.62` 以上或以下且模型置信度、舆情、大师框架形成交叉确认时，才给“预判低吸/预判减仓”等强动作。
4. 概率处于中性区时，给出明确的等待触发条件，不允许只写“看情况”。
5. 每次给结论时同时说明：如果价格没有触发，下一轮该怎么改判。

## 自进化规则

1. 每次给出前瞻判断前，同时查看 `reports/latest.json` 的 `learning` 字段和 `reports/memory/lessons.jsonl`。
2. 如果同一标的历史上多次出现“过度看多”或“过度看空”，本轮必须明确要求技术、舆情、大师框架至少两项交叉确认。
3. 预测核验后，优先报告方向是否命中、平均误差和 Brier 分数是否改善，而不是只说“这次判断如何”。
4. 发现同一错误连续出现两次时，把该错误升级为规则，写入新的经验教训，并在后续分析中自动执行。

## 趋势交易体系

在选股、判断趋势和确定买卖点前，按 `docs/交易体系.md` 的规则交叉检查：

1. 大势与板块趋势优于单只股票技术信号。
2. 下跌趋势不补仓；横盘等突破；上升趋势只做回调参与。
3. MA15 和分时均线只用于执行，不用于选股。
4. 涨停启动选股公式只做候选池，选出后还要人工剔除连板、凌乱和无人关注标的。
5. 抄底只看“个股低点抬高、指数仍新低”的相对强势背离，不接指数式下跌。

## 输出要求

回答必须直接、确定，不模棱两可。

至少给出：

- 当前结论：买入、卖出、持有、减仓、加仓、观察，只选一个明确动作
- 操作价格或区间
- 止损位和止盈/压力位
- 仓位参考
- 什么条件下改判
- 当前最重要的 1 到 3 个风险或催化

不允许使用“可能涨也可能跌”“建议谨慎”“自行判断”这类没有操作价值的空话。如果证据不足以判断，直接说明缺少哪一条信息，而不是含糊带过。
